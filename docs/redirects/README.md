# ProSporter legacy URL redirect map (CLNT-175)

Generated by `scripts/redirects/build_redirect_map.py` from the git-ignored
`exports/` snapshot (5 September 2026). Everything in this folder is derived and
contains no customer, order or account data.

| File | Purpose |
|---|---|
| `redirect-map.csv` | One row per normalized legacy path with exactly one outcome |
| `redirects.json` | The Next.js `redirects()` payload (nextjs-owned permanent redirects) |
| `gone.json` | Source paths that must answer 410 |
| `verification-report.md` | Output of `scripts/redirects/verify_redirects.py` |

## Outcomes

370 de-duplicated source URLs.

| Outcome | Count | Meaning |
|---|---:|---|
| `same_url` | 143 | Path preserved 1:1 in Next.js, must return 200 |
| `301` | 137 | Permanent redirect to a direct equivalent |
| `410` | 49 | Intentional retirement |
| `client_decision` | 35 | Ambiguous, blocked on the client |
| `held_redirect_to_collection` | 6 | Product held from the Shopify load; interim permanent redirect to its primary collection |

> `outcome = 301` means *permanent redirect*. Next.js `permanent: true` emits **308**
> (it preserves the request method); the `status_code` column records 308 for
> nextjs-owned rows and 301 for Shopify-owned rows. Both are permanent for SEO.

## Counts by source type

| Source type | Total | same_url | 301 | 410 | client_decision | held |
|---|---:|---:|---:|---:|---:|---:|
| `author_archive` | 1 | 0 | 0 | 1 | 0 | 0 |
| `feed` | 20 | 0 | 0 | 20 | 0 | 0 |
| `internal_link` | 1 | 0 | 1 | 0 | 0 | 0 |
| `page` | 30 | 9 | 4 | 13 | 4 | 0 |
| `post` | 15 | 0 | 14 | 0 | 1 | 0 |
| `post_category` | 7 | 0 | 7 | 0 | 0 | 0 |
| `post_tag` | 9 | 0 | 9 | 0 | 0 | 0 |
| `product` | 161 | 133 | 0 | 0 | 22 | 6 |
| `product_brand` | 1 | 0 | 0 | 0 | 1 | 0 |
| `product_category` | 30 | 0 | 29 | 0 | 1 | 0 |
| `product_tag` | 75 | 0 | 64 | 5 | 6 | 0 |
| `search` | 1 | 0 | 0 | 1 | 0 | 0 |
| `theme_taxonomy` | 1 | 0 | 0 | 1 | 0 | 0 |
| `woo_system` | 9 | 1 | 0 | 8 | 0 | 0 |
| `yoast_redirect` | 9 | 0 | 9 | 0 | 0 | 0 |

## Held products (not loaded to Shopify yet)

A legacy `/product/<slug>` path may only be graded `same_url` if the product is
really on the storefront. The migration holds a record back from the load while it
carries an unresolved blocking exception, and a held product has no Shopify product
and therefore no product page - so `same_url` would promise a 200 that is actually a
404. That was QA defect **D4**: six indexed legacy product URLs recorded as
`same_url, 200` returned 404 on the deployed storefront.

### The rule

On every run the builder reconciles each `same_url` product row against two
signals. A product is **held** when either is true:

1. `docs/migration/exception-register.csv` has an unresolved row for it whose code
   is `record_held_from_load` or whose severity is `critical`.
2. A migration load ledger is available and the handle is not in it (`exports/migration/live-store/store.json`).

A held row becomes:

- `outcome = held_redirect_to_collection`, `status_code = 308`, `owner = nextjs`
- `destination` = the product's **primary collection**, so the link keeps its
  equity and the customer lands on the nearest genuine listing rather than a 404
- `needs_client_decision = true`, and the `reason` and `evidence` columns name the
  exception that holds it (`<code>:product:<woo id>`)

The redirect is **interim**. Both signals are re-read on every run, so once the
client unblocks the product and it loads, the next rebuild puts the row back to
`same_url` on its own - nothing is hand edited. **Rebuild this map after any held
product is loaded**, otherwise the 308 will keep shadowing the new product page.

Primary collection is taken from the same IA assignment the migration transform
uses (`scripts/migration/normalize.py`: `assign_clubs`, `assign_product_type`,
`assign_surface`), with precedence **club > product type from a real category >
surface > inferred type > `/shop`**. The club collection is the most specific
listing a customer can be sent to; `beach`/`indoor` are the broadest axis, so they
only win when the type had to be guessed from the product name.

### Held rows in this map

| Legacy path | Destination | Held by |
|---|---|---|
| `/product/inner-west-jersey` | `/shop/clubs/inner-west-volley` | `attribute_needs_decision:product:6763` |
| `/product/modena-jersey` | `/shop/jerseys` | `attribute_needs_decision:product:6748` |
| `/product/modena-volley-jersey` | `/shop/clubs/teamwear` | `attribute_needs_decision:product:3154` |
| `/product/modena-volley-shorts` | `/shop/clubs/teamwear` | `attribute_needs_decision:product:3173` |
| `/product/provolley-jersey` | `/shop/clubs/provolley-academy` | `attribute_needs_decision:product:6718` |
| `/product/provolley-womens-jersey` | `/shop/clubs/provolley-academy` | `attribute_needs_decision:product:6758` |

Held products whose legacy path was already `client_decision` (a draft or a
duplicate handle) keep that outcome: they have no public legacy URL to preserve, so
there is nothing to redirect. `hoodie-pants-winter-2024` (an Easy Product Bundles
record with no Shopify equivalent) is the one such row today.

## Source inventory

Built from, in registration order (first registration wins the disposition,
later discoveries only add evidence):

1. Every product permalink, any status (`exports/products.json`).
2. Yoast Premium `_yoast_post_redirect_info` origins found on products.
3. Every page permalink, any status (`exports/pages.json`).
4. Every post permalink (`exports/posts.json`).
5. Every `product_cat` term URL - hierarchical path *and* bare-slug alias.
6. Every `product_tag` term URL.
7. Post category, post tag, author and feed archive URLs.
8. WooCommerce/WordPress system URLs (cart, checkout, my-account and endpoints,
   wishlist, shop, order-received, search).
9. Every Yoast sitemap URL not already covered.
10. Every navigation menu item URL.
11. Every internal link found in page/post HTML.

### Normalization

Legacy host stripped, path lowercased and percent-decoded, repeated slashes
collapsed, trailing slash removed (except the root), tracking parameters dropped.
Asset, `wp-json` and `wp-admin` URLs are excluded, as are on-page plugin action
links whose query is entirely made of action parameters (`add-to-cart`, `add_to_wishlist`, `filter_`, `filtering`, `max_price`, `min_price`, `orderby`, `product-page`, `quick_view_button`, `really_curr_tax`, `remove_item`, `removed_item`, `undo_item`, `wc-ajax`).
Those come from the Wishlist and Quick View plugins, both of which the audit
excluded from the migration; they are never indexed and never linked off-site.

### Trailing slash

Every legacy URL ends in `/`. Out of the box Next.js normalizes that away with
its own 308 **before** matching `redirects()` and before `src/proxy.ts` runs, so a
`source` written with a trailing slash is unreachable (verified against a running
server: it 404s without the slash and only ever 308s to the slash-free form).
Sources are therefore stored slash-free, which is the only form that matches, and
a legacy `/path/` request costs one platform normalization hop before the
redirect. That is not a chain in the map; `verification-report.md` counts it
separately.

The fix is `skipTrailingSlashRedirect: true` in `next.config.ts`. That hands the
normalization to `src/proxy.ts`, which strips the slash itself, looks the path up
in `redirects.json` / `gone.json`, and answers with the final destination (or a
410) in **one** hop; anything that is not a legacy URL still gets the ordinary 308
to its slash-free form, so canonical URLs are unchanged. Measured both ways on the
same tree: without the flag 175 rows cost two hops and no 410 row answers 410
without a hop first; with it, 231 rows are single-hop, 48 answer 410 directly and
nothing is multi-hop. Rewriting the sources to match both forms cannot work,
because the normalization happens before matching. Stripping the slash at the
CDN/edge is the alternative if the flag is unwanted.

### Query parameters

Allowlist preserved for attribution: `utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`, `utm_id`, `gclid`, `fbclid`.

Two things to know:

- These parameters are removed from the **inventory key** so that
  `/product/x?utm_source=meta` and `/product/x` are one row.
- At request time Next.js `redirects()` forwards **all** query parameters to the
  destination. Dropping the non-allowlisted ones needs a proxy/edge rule and is
  deliberately out of scope here; the allowlist is what analytics should honour.
  Query-only legacy URLs (`/?s=`, `/?page_id=N`) cannot be matched by a path
  redirect at all and are excluded from `redirects.json`.

## Category mapping (`/product-category/<slug>/` -> approved collection)

Reuses the `TYPE_MAP` in `mock-data/build_taxonomy.py`, with Protective Gear and
Coaching folded into Accessories (commits `6293089`, `fcc9c6d`). Each legacy
category maps to exactly one collection. Both the hierarchical URL
(`/product-category/beach-volleyball/top-crop/`) and the bare alias
(`/product-category/top-crop/`) are in the map.

| Legacy category | Products | Destination | Rationale |
|---|---:|---|---|
| `backpacks-bags` | 3 | `/shop/accessories` | type: Backpacks & Bags -> Accessories |
| `ball-cart-trolley` | 2 | `/shop/accessories` | coaching/equipment singleton folded into Accessories |
| `beach-volleyball` | 20 | `/shop/beach` | surface axis -> Beach collection |
| `bikini-bottoms` | 7 | `/shop/shorts-pants` | type axis wins over surface: Bikini Bottoms -> Shorts & Pants |
| `coaching` | 3 | `/shop/accessories` | coaching folded into Accessories |
| `elbow-pads` | 1 | `/shop/accessories` | protective gear folded into Accessories |
| `hoodies` | 7 | `/shop/hoodies-jackets` | type: Hoodies -> Hoodies & Jackets |
| `inner-west-volley` | 5 | `/shop/clubs/inner-west-volley` | club axis -> Inner West Volley |
| `jackets` | 5 | `/shop/hoodies-jackets` | type: Jackets -> Hoodies & Jackets |
| `jerseys` | 8 | `/shop/jerseys` | type: Jerseys -> Jerseys |
| `kneepads` | 3 | `/shop/accessories` | protective gear folded into Accessories |
| `pants` | 5 | `/shop/shorts-pants` | type: Pants -> Shorts & Pants |
| `polo-shirts` | 3 | `/shop/tops` | type: Polo Shirts -> Tops |
| `provolley-academy` | 13 | `/shop/clubs/provolley-academy` | club axis -> ProVolley Academy |
| `rain-jacket` | 3 | `/shop/hoodies-jackets` | type: Rain Jacket -> Hoodies & Jackets |
| `shorts` | 24 | `/shop/shorts-pants` | type: Shorts -> Shorts & Pants |
| `singlets` | 5 | `/shop/tops` | type: Singlets -> Tops |
| `sleeves` | 40 | `/shop/accessories` | protective gear folded into Accessories |
| `socks` | 4 | `/shop/accessories` | type: Socks -> Accessories |
| `sweat-suit` | 1 | `/shop/hoodies-jackets` | singleton legacy category folded into Hoodies & Jackets (IA-SPEC rule 5) |
| `sweater` | 3 | `/shop/hoodies-jackets` | type: Sweater -> Hoodies & Jackets |
| `t-shirts` | 11 | `/shop/tops` | type: T-Shirts -> Tops |
| `teamwear` | 26 | `/shop/clubs/teamwear` | club axis -> Clubs & Teams / Teamwear |
| `top-crop` | 8 | `/shop/tops` | type: Top Crop -> Tops |
| `women` | 7 | *client decision* | gender is a filter axis in the approved IA, not a collection route; client to choose a filtered landing page or retirement |

## Tag mapping (`/product-tag/<slug>/`)

Rule-based, evaluated in this order. Precedence is club > surface > type,
so `provolley-polo` lands on the ProVolley collection rather than Tops.

| Rule | Matches | Destination |
|---|---|---|
| Club token | `inner-west-volley`, `provolley`, `teamwear` | the club collection |
| Surface token | `beach`, `indoor` | the surface collection |
| Type token | `jersey`, `uniform`, `polo`, `t-shirt`, `tshirt`, `shirt`, `singlet`, `crop`, `hoodie`, `jacket`, `sweater`, `jumper`, `tracksuit`, `short`, `pant`, `sock`, `bag`, `backpack`, `kneepad`, `knee-pad` | the type collection |
| Brand name | `nine-australia`, `nine-volleyball-apparel`, `ninesquared`, `prosporter`, `sette`, `varone` | *client decision* (no brand collection in the approved IA) |
| Internal flag | `homepage` | 410 |
| Empty and unindexed | count 0 and absent from the sitemap | 410 |
| Everything else | geographic/marketing SEO tags | `/shop` (Shop All), flagged for the client |

`uniform` resolves to `/shop/jerseys` (a volleyball uniform is a playing kit) and
is flagged so the client can confirm.

Resulting tag destinations:

| Tag | Products | Outcome | Destination |
|---|---:|---|---|
| `auckland` | 1 | `301` | `/shop` |
| `australia` | 1 | `301` | `/shop` |
| `beach` | 10 | `301` | `/shop/beach` |
| `brisbane` | 1 | `301` | `/shop` |
| `comfort` | 0 | `410` | - |
| `custom-made-jersey` | 1 | `301` | `/shop/jerseys` |
| `custom-volleyball-jersey` | 2 | `301` | `/shop/jerseys` |
| `homepage` | 16 | `410` | - |
| `hoodie` | 0 | `301` | `/shop/hoodies-jackets` |
| `inner-west-volley` | 7 | `301` | `/shop/clubs/inner-west-volley` |
| `jersey` | 0 | `301` | `/shop/jerseys` |
| `melbourne` | 1 | `301` | `/shop` |
| `men-volleyball-jersey` | 1 | `301` | `/shop/jerseys` |
| `mens-volleyball-jersey` | 1 | `301` | `/shop/jerseys` |
| `mens-volleyball-uniform` | 2 | `301` | `/shop/jerseys` |
| `new-zealand` | 1 | `301` | `/shop` |
| `nine-australia` | 1 | `client_decision` | - |
| `nine-volleyball-apparel` | 2 | `client_decision` | - |
| `ninesquared` | 5 | `client_decision` | - |
| `ninesquared-polo` | 2 | `301` | `/shop/tops` |
| `ninesquared-sweater` | 1 | `301` | `/shop/hoodies-jackets` |
| `nsw-volleyball` | 2 | `301` | `/shop` |
| `pants` | 0 | `301` | `/shop/shorts-pants` |
| `perth` | 1 | `301` | `/shop` |
| `prosporter` | 1 | `client_decision` | - |
| `provolley` | 5 | `301` | `/shop/clubs/provolley-academy` |
| `provolley-polo` | 2 | `301` | `/shop/clubs/provolley-academy` |
| `provolley-sweater` | 1 | `301` | `/shop/clubs/provolley-academy` |
| `queensland-volleyball` | 2 | `301` | `/shop` |
| `set` | 0 | `410` | - |
| `sette` | 4 | `client_decision` | - |
| `shorts` | 14 | `301` | `/shop/shorts-pants` |
| `socks` | 1 | `301` | `/shop/accessories` |
| `south-australia-volleyball` | 2 | `301` | `/shop` |
| `sportswear` | 1 | `301` | `/shop` |
| `svl-jersey` | 4 | `301` | `/shop/jerseys` |
| `sydney` | 1 | `301` | `/shop` |
| `t-shirt` | 2 | `301` | `/shop/tops` |
| `teamwear` | 1 | `301` | `/shop/clubs/teamwear` |
| `teamwear-australia` | 2 | `301` | `/shop/clubs/teamwear` |
| `teamwear-new-zealand` | 2 | `301` | `/shop/clubs/teamwear` |
| `teamwear-volleyball` | 1 | `301` | `/shop/clubs/teamwear` |
| `unisex-volleyball-jumper` | 1 | `301` | `/shop/hoodies-jackets` |
| `varone` | 2 | `client_decision` | - |
| `victoria-volleyball` | 2 | `301` | `/shop` |
| `volleyball` | 0 | `410` | - |
| `volleyball-adelaide` | 2 | `301` | `/shop` |
| `volleyball-apparel-australia` | 1 | `301` | `/shop` |
| `volleyball-apparel-new-zealand` | 1 | `301` | `/shop` |
| `volleyball-auckland` | 2 | `301` | `/shop` |
| `volleyball-australia` | 1 | `301` | `/shop` |
| `volleyball-brisbane` | 2 | `301` | `/shop` |
| `volleyball-clothing-brisbane` | 1 | `301` | `/shop` |
| `volleyball-clothing-melbourne` | 1 | `301` | `/shop` |
| `volleyball-clothing-perth` | 1 | `301` | `/shop` |
| `volleyball-clothing-sydney` | 1 | `301` | `/shop` |
| `volleyball-jersey` | 1 | `301` | `/shop/jerseys` |
| `volleyball-jersey-australia` | 2 | `301` | `/shop/jerseys` |
| `volleyball-jersey-new-zealand` | 2 | `301` | `/shop/jerseys` |
| `volleyball-jersey-sydney` | 4 | `301` | `/shop/jerseys` |
| `volleyball-jumper-australia` | 1 | `301` | `/shop/hoodies-jackets` |
| `volleyball-melbourne` | 2 | `301` | `/shop` |
| `volleyball-perth` | 2 | `301` | `/shop` |
| `volleyball-polo-australia` | 2 | `301` | `/shop/tops` |
| `volleyball-polo-new-zealand` | 2 | `301` | `/shop/tops` |
| `volleyball-sweater-australia` | 1 | `301` | `/shop/hoodies-jackets` |
| `volleyball-sydney` | 2 | `301` | `/shop` |
| `volleyball-teamwear` | 1 | `301` | `/shop/clubs/teamwear` |
| `volleyball-training-sweater` | 1 | `301` | `/shop/hoodies-jackets` |
| `volleyball-uniform` | 3 | `301` | `/shop/jerseys` |
| `volleyball-uniform-australia` | 3 | `301` | `/shop/jerseys` |
| `warmth` | 0 | `410` | - |
| `western-australia-volleyball` | 2 | `301` | `/shop` |
| `womens-volleyball-jersey` | 2 | `301` | `/shop/jerseys` |
| `womens-volleyball-uniform` | 2 | `301` | `/shop/jerseys` |

## Blog decision

The storefront blog is a **single flat index at `/blog`**. Posts keep their handle
under the prefix (`/<slug>/` -> `/blog/<slug>`). There are no
`/blog/category/<slug>` or `/blog/tag/<slug>` routes, so every legacy
`/category/<slug>/` and `/tag/<slug>/` archive redirects to `/blog`, and author
archives and feeds are 410. This is applied consistently across all rows.

8 posts are WordPress theme demo content (beauty/skin care)
unrelated to the ProSporter brand. They are mapped to `/blog/<slug>` so nothing
breaks, but flagged: the recommendation is to retire them (410) and drop them
from the sitemap.

- `/authentic-brands`
- `/ethical-skin-care`
- `/everything-about-skin`
- `/growing-categories`
- `/hair-care-organic`
- `/latest-make-up-trends`
- `/led-light-therapy`
- `/superfood-beauty`

## Client decisions required

35 URLs have **no** destination until the client decides:

| Source | Type | Reason |
|---|---|---|
| `/1687-2` | `page` | untitled duplicate page containing only WooCommerce filter widgets; client to confirm retirement |
| `/?page_id=1692` | `page` | draft page with no slug and no public path; query-only legacy URL cannot be matched by a path redirect |
| `/?page_id=2535` | `page` | draft page with no slug and no public path; query-only legacy URL cannot be matched by a path redirect |
| `/?page_id=3276` | `page` | draft page with no slug and no public path; query-only legacy URL cannot be matched by a path redirect |
| `/109` | `post` | numeric post slug with no title-derived handle; client to approve a clean /blog/<slug> handle or retirement |
| `/product/alex-man` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/alex-woman` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/calle-2` | `product` | dirty '-2' handle with no canonical sibling; client to approve a clean Shopify handle (then the legacy path 301s to it) |
| `/product/cinque-t-shirt-man` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/cinque-t-shirt-woman` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/collar-zip-sub-unisex` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/deck-2` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/deva-unisex` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/hoodie-pants-winter-2024` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/pipe-2-man` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/pipe-2-woman` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/provolley-training-t-shirt-zero-2-yellow` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/quick-unisex` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/rich-unisex` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/royal-male-t-shirt-2` | `product` | duplicate handle: 'royal-male-t-shirt-2' and 'royal-male-t-shirt' are both live products; client to pick the canonical handle before the loser is 301'd |
| `/product/sette-man` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/sette-woman` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/tre-man` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/tre-woman` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/velo-collar-zip` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/velo-jacket` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/product/warm-up-unisex` | `product` | draft product has no public legacy URL; client to confirm whether it is published on Shopify (then same path) or retired |
| `/brand/ninesquared` | `product_brand` | product brand archive; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-category/women` | `product_category` | gender is a filter axis in the approved IA, not a collection route; client to choose a filtered landing page or retirement |
| `/product-tag/nine-australia` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-tag/nine-volleyball-apparel` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-tag/ninesquared` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-tag/prosporter` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-tag/sette` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |
| `/product-tag/varone` | `product_tag` | brand tag; the approved IA has no brand collection - client to decide a Shopify vendor collection or retirement |

A further 44 URLs have a working destination but are flagged for
confirmation (they are live in `redirects.json` today):

| Source | Destination | Reason |
|---|---|---|
| `/authentic-brands` | `/blog/authentic-brands` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/ethical-skin-care` | `/blog/ethical-skin-care` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/everything-about-skin` | `/blog/everything-about-skin` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/growing-categories` | `/blog/growing-categories` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/hair-care-organic` | `/blog/hair-care-organic` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/latest-make-up-trends` | `/blog/latest-make-up-trends` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/led-light-therapy` | `/blog/led-light-therapy` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/superfood-beauty` | `/blog/superfood-beauty` | post handle preserved under the /blog prefix; theme demo content unrelated to ProSporter - recommend retirement, client to confirm |
| `/product/inner-west-jersey` | `/shop/clubs/inner-west-volley` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (club axis: inner-west-volley); held by exception attribute_needs_decision:product:6763; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product/modena-jersey` | `/shop/jerseys` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (type axis: jerseys (from category)); held by exception attribute_needs_decision:product:6748; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product/modena-volley-jersey` | `/shop/clubs/teamwear` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (club axis: teamwear); held by exception attribute_needs_decision:product:3154; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product/modena-volley-shorts` | `/shop/clubs/teamwear` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (club axis: teamwear); held by exception attribute_needs_decision:product:3173; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product/provolley-jersey` | `/shop/clubs/provolley-academy` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (club axis: provolley-academy); held by exception attribute_needs_decision:product:6718; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product/provolley-womens-jersey` | `/shop/clubs/provolley-academy` | product is held from the Shopify load, so /product/<slug> has no page; interim permanent redirect to its primary collection (club axis: provolley-academy); held by exception attribute_needs_decision:product:6758; the row returns to same_url once the product is loaded and this map is rebuilt |
| `/product-tag/auckland` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/australia` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/brisbane` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/melbourne` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/mens-volleyball-uniform` | `/shop/jerseys` | type axis: tag contains 'uniform' |
| `/product-tag/new-zealand` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/nsw-volleyball` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/perth` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/queensland-volleyball` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/south-australia-volleyball` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/sportswear` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/sydney` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/victoria-volleyball` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-adelaide` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-apparel-australia` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-apparel-new-zealand` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-auckland` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-australia` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-brisbane` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-clothing-brisbane` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-clothing-melbourne` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-clothing-perth` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-clothing-sydney` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-melbourne` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-perth` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-sydney` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/volleyball-uniform` | `/shop/jerseys` | type axis: tag contains 'uniform' |
| `/product-tag/volleyball-uniform-australia` | `/shop/jerseys` | type axis: tag contains 'uniform' |
| `/product-tag/western-australia-volleyball` | `/shop` | geographic/marketing SEO tag with no approved collection; Shop All is the nearest genuine listing (never the home page) |
| `/product-tag/womens-volleyball-uniform` | `/shop/jerseys` | type axis: tag contains 'uniform' |

## Still to be merged in manually

This map is complete for everything the authenticated exports expose. Three
sources are **not** in it and must be merged before cutover:

1. **Yoast Premium redirect manager export.** Yoast's redirect table is not
   exposed over REST (audit README finding 12). Only the 10 redirects stored as
   product meta (`_yoast_post_redirect_info`) are captured here. Export the full
   table from SEO -> Redirects as CSV and re-run the builder with it merged, so
   any legacy chain collapses to the final destination in one hop.
2. **GA4 landing pages.** Pull the last 12-16 months of landing-page URLs and add
   any that are not already rows here; they are the URLs with real traffic and
   should be prioritised for a genuine destination over a generic listing.
3. **Search Console.** Export Performance -> Pages and the Indexing report; add
   indexed URLs missing from the sitemaps, and use impressions to re-rank the
   client decisions above.

Also worth a crawl of the live site before cutover to catch URLs that exist only
in theme templates or widgets rather than in page/post HTML.

## Rerun and verify

```bash
# 1. Rebuild the map from exports/ (deterministic)
python3 scripts/redirects/build_redirect_map.py

# 1b. Offline check, no server needed: every same_url row is a product the
#     migration really loaded (catches a held product straight away)
python3 scripts/redirects/verify_redirects.py --ledger-only

# 2. Build and start the app (next.config.ts reads docs/redirects/redirects.json)
SHOPIFY_OPTIONAL=1 npm run build
SHOPIFY_OPTIONAL=1 NODE_ENV=production PORT=3114 npx next start &

# 3. Verify every row against the running server
python3 scripts/redirects/verify_redirects.py --base-url http://localhost:3114

# 4. Stop the server
kill %1
```

The verifier writes `verification-report.md` and exits non-zero on a real
failure. Destinations that 404 only because the page is not built in the
prototype yet are reported separately and do not fail the run. A `same_url`
row whose handle is not in the migration load ledger is a real failure; step 1b
runs that one check alone and needs neither a build nor a server.

## Implementation

- `next.config.ts` imports `redirects.json` (142 rules) and returns it from `redirects()`.
- `src/proxy.ts` imports `gone.json` (48 paths) and answers each one with a
  real 410, a small retired-page body, `Cache-Control: public, max-age=3600,
  must-revalidate` (never `immutable`, so wiring a real page later is not stuck
  behind a CDN cache) and `X-Robots-Tag: noindex`. The same file also imports
  `redirects.json`, which is what lets it resolve a trailing-slash legacy URL to
  its final destination in one hop once `skipTrailingSlashRedirect: true` is set.
  The one 410 row not in `gone.json` is `/?s=`, a query-only URL no path rule can
  match.
- `src/app/blog/page.tsx` and `src/app/blog/[slug]/page.tsx` are **placeholders**
  so that the 31 redirects pointing at `/blog` and `/blog/<slug>` land on a 200
  rather than a 404. They render "coming soon", carry `robots: noindex`, and the
  post route serves only the slugs named in `redirects.json` (anything else 404s).
  Delete them when the real blog lands.
- Rows owned by `shopify` are listed for completeness. If the legacy paths keep
  resolving to the Next.js app after cutover, Next serves the 410; if any of them
  is pointed at Shopify instead, Shopify owns the response.
